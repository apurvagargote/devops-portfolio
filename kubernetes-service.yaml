apiVersion: v1
kind: Service
metadata:
  name: portfolio-website
  namespace: portfolio
  labels:
    app: portfolio-website
spec:
  selector:
    app: portfolio-website
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP