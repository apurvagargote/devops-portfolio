[jenkins]
jenkins-server ansible_host=<jenkins_ip> ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/portfolio-key.pem

[eks_nodes]
# These will be dynamically populated by the Jenkins pipeline